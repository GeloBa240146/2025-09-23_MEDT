let start = prompt("Geben Sie den Start ein:");
let end = prompt("Geben Sie die letzte zahl ein:");

let wert1;
let wert2;

do {
    wert1 = prompt("Geben Sie den Start ein:");
    wert2 = prompt("Geben Sie die letzte zahl ein:");
} while(wert2 < wert1);



for (let i = 0; i >= start && i <= end; i++) {
    if (i % 2 == 0) {
        i;
    }
}